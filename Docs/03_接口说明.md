# 3 接口说明

这里主要介绍 PaceCatLidarSDK类中对外调用的接口。

***#include<pacecatlidarsdk.h>***

## 枚举

```C++
enum LidarState
{
    OFFLINE = 0,//离线
    ONLINE,//在线
    QUIT//退出
};
```

## 结构体

```C++

struct CmdHeader{
    unsigned short sign; //与硬件约定的标志位
    unsigned short ver;//协议版本
    unsigned short msgid;//消息ID
    unsigned short cmd;//命令
    unsigned int len;//命令长度
};

```

```C++

struct Point3D{
    float x;//点云坐标系 x单位m
    float y;//点云坐标系 y单位m
    float z;//点云坐标系 y单位m
};

```

```C++

struct onePoi{
    uint16_t row_pos;//该点所在的行下标
    uint16_t col_pos;//该点所在的列下标
    float distance;// 距离，单位 cm
    uint32_t reflectivity; // 反射率
    Point3D pt3d;//真实坐标值
    uint32_t peak0_pos;// 峰 1 的位置
    uint32_t peak0_val;// 峰 1 的高度
    uint32_t peak1_pos;// 峰 2 的位置
    uint32_t peak1_val;// 峰 2 的高度
    uint16_t envSumXi;
    uint32_t nbVal;
    uint32_t centDis;// 质心距离
    uint32_t riseDis;// 上升沿距离
    uint32_t firPeak;// FIR 峰高
    uint64_t timestamp; // 时间戳，单位us
};

```

```C++

//通用包头
typedef struct tag_soc_head{ // size 6
    uint16_t head;// 固定包头，0x5A,0xA5
    uint8_t version_major; // 协议主版本，当前为 1
    uint8_t data_type;// 数据类型，点云数据 0x01 0x0c 0x0d 0x0e
    uint16_t reserved;// 保留字段
} soc_head_t;

```

```C++
// 点云数据包头(在通用包头之后)
typedef struct tag_soc_protocol_data{// size 40
    uint8_t version_minor;// 协议子版本，当前为 0
    uint16_t data_sequence; // 数据序列，可用于统计接收数据是否丢包
    uint16_t frame_cnt;// 帧序号
    uint8_t time_type;// 时间戳类型
    uint16_t col;// 当前数据包的起始 col
    uint16_t row;// 当前数据包的起始 row
    uint16_t dot_num;// 当前数据包的 data 字段包含的点的数量
    uint8_t type;// 点云数据类型，（Distance/XYZ/(Distance+XYZ) ）+ 反射率 + 标签，默认 XYZ
    uint8_t echo_num;// 点云数据的回波个数，默认双回波，最大支持回波个数是 4 个回波。分别是最强、强、最近、最远。
    uint8_t frame_flag;// 0 帧起始下标 1 帧中间序号 2:帧结束下标   
    uint8_t reserved[11];// 保留字段
    uint8_t crc8;// 主协议头+子协议头的检验码，使用 crc-8 算法
    uint32_t crc32;// timestamp+data 段校验码，使用 CRC-32 算法
    uint64_t timestamp;// 时间戳，单位微秒
    uint8_t data[0];// 数据信息
} soc_protocol_data_t;

```

```C++

// 具体点数据(在点云数据包头之后)
typedef struct tag_soc_dis_pt_data{
    uint16_t distance; // 当前回波的距离值
    int16_t x;//点坐标的x值
    int16_t y;//点坐标的y值
    int16_t z;//点坐标的z值
    uint8_t reflect; // 当前回波的反射率
    uint8_t label;// 保留，跟点云测量到的实际物体有关，如雨雾、灰尘等
} soc_dis_pt_data_t;

```

```C++
//imu数据包头(在通用包头之后)
typedef struct {
    uint8_t  crc8_header;//校验头(暂无)
    uint8_t  crc8_imu_dat;//校验数据(暂无)
    uint8_t  version;//imu版本   2/3
    uint16_t len;//包长度
    uint8_t  time_type;//时间戳类型(暂无)
    int64_t  timestamp;//时间戳
    int16_t  temperature;//温度   单位: 1/256 ℃
    uint8_t  data[0];
} fs_lidar_imu_header_t;

```

```C++
//imu具体数据(在imu数据包头之后) 对应上述结构version = 2
typedef struct {
    float gyro_x; // 角速度:°/s
    float gyro_y; // 角速度:°/s
    float gyro_z; // 角速度:°/s
    float acc_x;  // 加速度:g
    float acc_y;  // 加速度:g
    float acc_z;  // 加速度:g
} fs_lidar_imu_v2;
```

```C++
//imu具体数据(在imu数据包头之后) 对应上述结构version = 3
typedef struct {
    float pitch;//俯仰角  根据X轴旋转
    float roll;//横滚角 根据Z轴旋转
    float yaw;//偏航角 根据Y轴旋转
    float gyro_x; // 角速度:rad/s
    float gyro_y; // 角速度:rad/s
    float gyro_z; // 角速度:rad/s
    float acc_x;  // 加速度:g
    float acc_y;  // 加速度:g
    float acc_z;  // 加速度:g
} fs_lidar_imu_v3;
    
```

```C++
//imu整合后发布的结构
typedef struct {
    int64_t  timestamp;//时间戳
    uint8_t  version;//发布版本(不同版本代表不同的imu硬件)
    float pitch;//俯仰角  根据X轴旋转
    float roll;//横滚角 根据Z轴旋转
    float yaw;//偏航角 根据Y轴旋转
    float gyro_x; // 角速度:rad/s |°/s
    float gyro_y; // 角速度:rad/s |°/s
    float gyro_z; // 角速度:rad/s |°/s
    float acc_x;  // 加速度:g
    float acc_y;  // 加速度:g
    float acc_z;  // 加速度:g
    
} fs_lidar_imu_t;

```

```C++
//设备参数
struct DEV_CFG_ST
    {
        uint16_t cfg_ver;//参数结构体版本号

        char dev_type[16];//型号 :LDS-S00-E
        char dev_sn[32];//设备SN号
        uint32_t dev_id_num; //设备ID号 

        uint32_t soft_ver; //软件版本号
        uint32_t hard_ver;//硬件版本号

        uint8_t local_ip[4]; //设备ip
        uint8_t local_mask[4]; //设备掩码
        uint8_t local_gateway[4];//设备网关
        uint16_t local_port;//设备监听端口

        uint8_t upload_ip[4];//上传目标 ip
        uint16_t upload_port;//上传目标 端口 
        uint16_t upload_point_clound_en : 3;//数据输出模式 0 不上传 1单回波 360*150;2 单回波抽点360*75
        uint16_t upload_dev_status_en : 1;//设备信息上传开关
        uint16_t upload_alarm_info_en : 1;//报警包信息上传开关
        uint16_t upload_type_reserve : 11;//预留位
        uint8_t reserve[524];

    };

```

```C++
//心跳包
typedef struct{
    char protocol_header[2];//协议头  默认LD
    uint16_t protocol_ver;//协议版本
    TIME_ST timestamp;//时间戳   秒+纳秒(秒后的数据转为纳秒)
    unsigned char dev_sn[20];//sn号
    unsigned char dev_type[16];//设备型号
    uint32_t software_ver;//软件版本号
    uint32_t dev_id;//设备ID
    uint8_t  ip[4];//设备ip
    uint8_t  mask[4];//设备掩码
    uint8_t  gateway[4];//设备网关
    uint16_t listen_port;//服务端口
    uint8_t  remote_ip[4];//上传IP
    uint16_t remote_port;//上传端口
    uint32_t status;//状态
    char reserve[20];
    uint32_t crc;//校验
} DevHeart;

```

```C++
//报警包事件定义
typedef struct
{
  union
  {
    struct
    {
        uint32_t zone_observe : 1;//观察区报警       
        uint32_t zone_warning : 1;//警戒区报警      
        uint32_t zone_alarm : 1;//报警区报警
        uint32_t zone_cfg_invalid : 1;//无防区配置  
        uint32_t tx_tem_h : 1;//发射高温            
        uint32_t rx_tem_h : 1;//接收高温            
        uint32_t tx_vol_h : 1;//发射高压            
        uint32_t tx_vol_l : 1;//发射低压            
        uint32_t rx_vol_h : 1;//接收高压            
        uint32_t rx_vol_l : 1;//接收低压            
        uint32_t no_cali_table : 1;//无校准表参数      
        uint32_t data_is_zero : 1;//全零报警        
        uint32_t no_imu_data : 1;//IMU 无数据   

      uint32_t reserved : 19;           
    };
    uint32_t events;
  };
}__attribute__ ((packed)) PROCOTOL_ALARM_EVENTS_ST;


```

```C++
//报警包
typedef struct
{
  uint16_t id; //计数 id
  uint8_t actived_zone_num;//当前激活防区 0x00-0x0F
  uint8_t reserve;//预留位
  PROCOTOL_ALARM_EVENTS_ST events;//报警事件
  uint8_t reserved[300];
}__attribute__ ((packed)) PROTOCOL_DATA_PACK_ALARM_ST;

```

```C++
//对外保存的网络信息结构
struct NetWorkInfo {
    uint32_t lidar_ip;//雷达ip
    uint32_t host_ip;//上传ip
    uint16_t lidar_port;//雷达端口
    uint16_t host_port;//雷达端口
};

```

## 函数指针

```C++
@名称   LidarCloudPointCallback
@简介   设置点云数据的函数指针
@参数   uint32_t handle                 雷达ID
@参数   const uint8_t dev_type          数据类型
@参数   onePoi*data                     帧数据指针
@参数   uint16_t num                    帧数据的点数
@参数   void* client_data               用户需要全局传递的数据指针
@返回   指针
@注释   用户线程传入
```

```C++
@名称   LidarImuDataCallback
@简介   设置IMU数据的函数指针
@参数   uint32_t handle             雷达ID
@参数   const uint8_t dev_type      数据类型
@参数   fs_lidar_imu_t *data        imu数据包的数据指针
@参数   void* client_data           用户需要全局传递的数据指针
@返回   指针
@注释   用户线程传入
```

```C++
@名称   LidarLogDataCallback
@简介   设置日志数据的函数指针
@参数   uint32_t handle             雷达ID
@参数   const uint8_t dev_type      数据类型
@参数   const char *data            用户层数据
@参数   int len                     用户层数据长度
@返回   指针
@注释   用户线程传入
```

## 函数

```C++
@名称   Init
@简介   类初始化，主要是启用心跳包线程
@参数   std::string adapter                 当前网卡名称(绑定网卡，避免多网卡心跳包接收异常)
@返回   void
@注释   实例运行第一步

```

```C++
@名称   Uninit
@简介   类反初始化，主要是关闭所有的工作线程
@返回   void
@注释   无

```

```C++
@名称   SetPointCloudCallback
@简介   设置点云数据的回调函数
@参数   int ID                      当前雷达的编号
@参数   LidarCloudPointCallback cb  回调函数的指针
@参数   void* client_data           用户需要全局使用的数据指针
@返回   bool
@注释   实例运行第三步
```

```C++
@名称   SetImuDataCallback
@简介   设置IMU数据的回调函数
@参数   int ID                      当前雷达的编号
@参数   LidarImuDataCallback cb     回调函数的指针
@参数   void* client_data           用户需要全局使用的数据指针
@返回   bool
@注释   实例运行第三步
```

```C++
@名称   SetLogDataCallback
@简介   设置日志数据的回调函数
@参数   int ID                      当前雷达的编号
@参数   LidarImuDataCallback cb     回调函数的指针
@参数   void* client_data           用户需要全局使用的数据指针
@返回   bool
@注释   实例运行第三步
```

```C++
@名称   WritePointCloud
@简介   写入点云数据
@参数   int ID                      当前雷达的编号
@参数   const uint8_t dev_type      数据类型
@参数   onePoi *data                点云帧指针
@参数   int     num                 点云数量
@返回   void
@注释   
```

```C++
@名称   WriteImuData
@简介   写入IMU数据
@参数   int ID                      当前雷达的编号
@参数   const uint8_t dev_type      数据类型
@参数   fs_lidar_imu_t *data        imu数据指针
@返回   void
@注释   
```

```C++
@名称   WriteLogData
@简介   写入日志数据
@参数   int ID                      当前雷达的编号
@参数   const uint8_t dev_type      数据类型
@参数   char* *data                 数据
@参数   int len                     数据长度
@返回   void
@注释   
```

```C++
@名称   AddLidar
@简介   添加雷达
@参数   std::string lidar_ip        雷达ip
@参数   int lidar_port              雷达端口
@参数   int listen_port             监听端口
@返回   int                         雷达ID
@注释   实例运行第二步
```

```C++
@名称   ConnectLidar
@简介   连接雷达,发送指令，接收数据
@参数   int ID                      雷达ID
@返回   bool
@注释   实例运行第五步
```

```C++
@名称   DisconnectLidar
@简介   断开连接雷达
@参数   int ID                      雷达ID
@返回   bool
@注释   
```

```C++
@名称   QueryBaseInfo
@简介   查看雷达基本参数
@参数   int ID                      雷达ID
@参数   NetWorkInfo info            雷达参数
@返回   bool
@注释   
```

```C++
@名称   QueryVersion
@简介   查看雷达版本
@参数   int ID                      雷达ID
@参数   VersionInfo info            雷达版本信息
@返回   bool
@注释   
```

```C++
@名称   QueryDeviceState
@简介   查询设备状态
@参数   int ID                      雷达ID
@返回   int                         雷达状态 LidarState
@注释   
```

```C++
@名称   SetLidarNetWork
@简介   设置雷达网络参数(未开放)
@参数   int ID                      雷达ID
@参数   std::string ip              网址
@参数   std::string mask            掩码
@参数   std::string gateway         网关
@参数   uint16_t port               端口
@返回   bool                        
@注释   
```

```C++
@名称   SetLidarUploadNetWork
@简介   设置雷达上传网络参数(未开放)
@参数   int ID                      雷达ID
@参数   std::string upload_ip       网址
@参数   uint16_t upload_port        端口
@返回   bool                        
@注释   
```

```C++
@名称   SetYawAngle
@简介   设置偏航角
@参数   int ID                      雷达ID
@参数   int yaw                     偏航角度
@返回   bool                        
@注释   
```

```C++
@名称   SetWorking
@简介   设置雷达启停
@参数   int ID                      雷达ID
@参数   bool isrun                  启动或者停止
@返回   bool                        
@注释   
```

```C++
@名称   SetTimeStampSync
@简介   设置时间同步
@参数   int ID                      雷达ID
@返回   bool                        
@注释   
```

```C++
@名称   QueryIDByIp
@简介   根据雷达ip获取ID
@参数   const char* lidar_ip         雷达IP
@返回   int                          雷达ID                  
@注释   
```

```C++
@名称   GetConfig
@简介   根据雷达ID获取配置信息
@参数   int ID                       雷达ID
@返回   RunConfig                    雷达配置信息                         
@注释   
```
